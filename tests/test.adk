l = [
'hi', 
'test'
]
stdout.write(l, '\n')

function x(val) as this { #Test
    stdout.write(val, '\n')
    return null #Note the static return value
}

y = x(8*"Hello")

if y == null { # x always returns null, y is always the return value of x, y always = null
    stdout.write(y, '\n')
}
