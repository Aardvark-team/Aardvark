stdout.write("Hello World!\n")

function fib(n) -> int {
  stdout.write(n, '\n')
  if n == 0 {
    return 0
  } else if n == 1 {
    return 1
  }
  return fib(n - 1) + fib(n - 2)
} 

let num = 3
fib(num)