stdout.write("Hello World!\n")

function fib(n) -> int {
  if n <= 0 {
    return 0
  } 
  if n is 1 {
    return 1
  }
  return fib(n - 1) + fib(n - 2)
}

num = 7
stdout.write('Fib of', num, 'is', fib(num), "\n")

# תֲ = 5
# stdout.write(תֲ, '\n')