class X as this {
  $constructor(x, y) {
    this.x = x
    this.y = y
    stdout.write('constructed!\n')
  }
  $call(x, y, z) {
    stdout.write('Args work?', x, y, z, '\n')
  }
  function method() {
    return this.x
  }
  attr = 5
}
